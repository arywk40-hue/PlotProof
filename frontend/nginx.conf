server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router routes must fall back to the SPA entry point on refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
