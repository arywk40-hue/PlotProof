# Required for VERIFIED submissions to be pinned to IPFS.
PINATA_JWT=replace_with_a_Pinata_JWT
# Optional: absolute path is recommended in a hosted environment.
PLOTPROOF_DB_PATH=data/plotproof.sqlite
PORT=4000
